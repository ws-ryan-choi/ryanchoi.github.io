export declare class IdentityTokenService {
    private privateKey;
    private issuer;
    private keyId;
    constructor(privateKey: string, issuer: string, keyId: string);
    getTokenForUser(email: string, role?: string): Promise<string>;
}
